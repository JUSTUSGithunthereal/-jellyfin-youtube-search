name: Build Plugin

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore src/Jellyfin.Plugin.YouTubeSearch/Jellyfin.Plugin.YouTubeSearch.csproj

      - name: Build
        run: dotnet publish src/Jellyfin.Plugin.YouTubeSearch/Jellyfin.Plugin.YouTubeSearch.csproj -c Release -o out

      - name: Zip
        run: |
          cd out
          zip -r ../plugin.zip .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin
          path: plugin.zip
